spring:
  application:
    name: simple-task-library
  datasource:
    driver-class-name: org.h2.Driver
    name: tasks_db
    url: jdbc:h2:file:~/tasks_db
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
